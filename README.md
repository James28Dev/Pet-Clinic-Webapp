# 🐾 ระบบจัดการคลินิกสัตว์ (Pet Clinic Management System)

[![Project Status](https://img.shields.io/badge/Status-Completed-brightgreen.svg)](YOUR_REPO_LINK)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](https://github.com/James28Dev/Pet-Clinic-Webapp/blob/main/LICENSE)

**Fontend:**
[![HTML5](https://img.shields.io/badge/html5-%23E34F26.svg)](https://html.spec.whatwg.org/multipage/)
[![CSS3](https://img.shields.io/badge/css3-%231572B6.svg)](https://www.w3.org/Style/CSS/)
[![JavaScript](https://img.shields.io/badge/javascript-%23323330.svg)](https://developer.mozilla.org/en-US/docs/Web/JavaScript)

**Backend:**
[![NodeJS](https://img.shields.io/badge/node.js-6DA55F.svg)](https://nodejs.org/en)
[![Express.js](https://img.shields.io/badge/express.js-%23404d59.svg)](https://expressjs.com/)

**Database:**
[![MySQL](https://img.shields.io/badge/mysql-4479A1.svg)](https://www.mysql.com/)

> โปรเจกต์รายวิชา**การพัฒนาเว็บแอปพลิเคชัน (Web Application Development)** และ **ระบบฐานข้อมูล (Database Systems)** ที่ **มหาวิทยาลัยราชภัฏสงขลา**
>
> ระบบนี้ถูกพัฒนาขึ้นเพื่อใช้ในการบริหารจัดการข้อมูลสำคัญภายในคลินิกสัตว์เลี้ยงแบบครบวงจร ตั้งแต่ข้อมูลเจ้าของสัตว์ สัตว์เลี้ยง การนัดหมายของสัตวแพทย์ ไปจนถึงการบันทึกประวัติการรักษา

---

## ✨ คุณสมบัติหลัก (Features)

ระบบจัดการคลินิกสัตว์มีฟังก์ชันหลักในการบริหารจัดการข้อมูล เพื่อให้การทำงานในคลินิกเป็นไปอย่างราบรื่น:

* **ระบบยืนยันตัวตน (Authentication):** รองรับการ **เข้าสู่ระบบ (Login)** และ **สมัครสมาชิก (Register)** สำหรับผู้ใช้งานหลายบทบาท (Admin, Veterinarian, Staff)
* **การจัดการผู้ใช้งาน (User Management):** ระบบจัดการข้อมูลบุคลากรในคลินิก (สัตวแพทย์และเจ้าหน้าที่)
* **การจัดการข้อมูลเจ้าของสัตว์ (Owner Management):** บันทึก เพิ่ม แก้ไข และลบข้อมูลเจ้าของสัตว์เลี้ยง
* **การจัดการข้อมูลสัตว์เลี้ยง (Pet Management):** บันทึกข้อมูลสัตว์เลี้ยงของเจ้าของแต่ละราย (ชื่อ, ชนิด, พันธุ์, เพศ, วันเกิด)
* **ระบบใบนัดหมาย (Appointment System):** จัดการตารางนัดหมายระหว่างเจ้าของ/สัตว์เลี้ยง กับสัตวแพทย์ พร้อมฟังก์ชันกรองข้อมูลตามช่วงวันที่
* **ประวัติการรักษา (Treatment Records):** บันทึกประวัติการวินิจฉัย การให้ยา/การรักษา โดยสามารถอ้างอิงถึงใบนัดหมายที่เกี่ยวข้องได้
* **อินเทอร์เฟซผู้ใช้ (UI):** ออกแบบด้วย HTML/CSS ที่เน้นความสะอาด ใช้งานง่าย และรองรับการแสดงผลบนอุปกรณ์ที่หลากหลาย (Responsive Design)

---

## 🛠️ เทคโนโลยีที่ใช้ (Built With)

โปรเจกต์นี้ใช้เทคโนโลยีหลักแบบ Full-Stack JavaScript โดยใช้ Node.js เป็น Runtime:

| ส่วน | เทคโนโลยี | รายละเอียด |
| :--- | :--- | :--- |
| **Frontend** | HTML, CSS | โครงสร้างและส่วนแสดงผลของหน้าเว็บ |
| **Styling** | Custom CSS | ใช้ CSS พื้นฐานเพื่อให้หน้าเว็บมีโทนสว่างและอ่านง่าย |
| **Client-side Logic** | JavaScript (Vanilla) | ใช้ Fetch API สำหรับการติดต่อ Backend และจัดการ DOM |
| **Backend** | Node.js + Express.js | สร้าง RESTful API สำหรับจัดการข้อมูล |
| **Database** | MySQL (ผ่าน `mysql2/promise`) | ใช้เก็บข้อมูลหลักทั้งหมดของระบบ |
| **Security** | `express-session`, `bcryptjs` | จัดการ Session และการเข้ารหัสรหัสผ่าน |

---

## 🚀 การเริ่มต้นใช้งาน (Getting Started)

โปรเจกต์นี้จำเป็นต้องติดตั้ง Node.js และระบบจัดการฐานข้อมูล MySQL ก่อน

### Prerequisites

1.  **Node.js และ npm:** ติดตั้ง [Node.js](https://nodejs.org/) (แนะนำเวอร์ชัน LTS)
2.  **MySQL Server:** ติดตั้งและรัน [MySQL](https://www.mysql.com/) บนเครื่องของคุณ

### Installation และ Running

1.  **Clone Repository:**
    ```bash
    git clone https://github.com/James28Dev/Pet-Clinic-Webapp.git
    cd Pet-Clinic-Webapp
    ```

2.  **ติดตั้ง Dependencies ของ Node.js:**
    ```bash
    npm install
    ```

3.  **ตั้งค่าฐานข้อมูล (Database Setup):**
    * **สร้าง Database:** เข้าสู่ MySQL และสร้าง Database ชื่อ `pet_clinic_db` (หรือชื่อตามที่กำหนดในไฟล์ `.env`)
    * **Import Schema:** รันสคริปต์ SQL ในไฟล์ `database.sql` เพื่อสร้างตารางและข้อมูลเริ่มต้น:
        ```bash
        mysql -u [DB_USER] -p pet_clinic_db < database.sql
        ```
    * **ตั้งค่าไฟล์ `.env`:** ตรวจสอบและแก้ไขไฟล์ `.env` ใน Root Directory ให้ตรงกับการตั้งค่า MySQL Server ของคุณ (โดยเฉพาะ `DB_USER` และ `DB_PASSWORD`):
        ```ini
        # ตัวอย่าง .env
        PORT=3000
        DB_HOST=localhost
        DB_USER=root
        DB_PASSWORD=your_password
        DB_NAME=pet_clinic_db
        ```

4.  **รันโปรเจกต์:**
    ```bash
    # สำหรับการรันแบบ Production
    npm start 
    # หรือ สำหรับการพัฒนา (ใช้ nodemon ถ้ามี)
    npm run dev 
    ```

5.  **เปิดหน้าเว็บ:** เปิดเบราว์เซอร์ไปที่:
    ```
    http://localhost:3000
    ```
    (หรือพอร์ตที่คุณตั้งค่าไว้ใน `.env`)

---

## 📂 โครงสร้างฐานข้อมูลหลัก (Core Database Structure)

ฐานข้อมูล `pet_clinic_db` ประกอบด้วยตารางหลักที่เชื่อมโยงกันดังนี้:

| ตาราง (Table) | บทบาท |
| :--- | :--- |
| `users` | ข้อมูลบัญชีผู้ใช้งานระบบ (Login) และสิทธิ์ (Admin, Vet, Staff) |
| `owners` | ข้อมูลเจ้าของสัตว์เลี้ยง (ชื่อ, โทรศัพท์, ที่อยู่) |
| `veterinarians` | ข้อมูลสัตวแพทย์ |
| `pets` | ข้อมูลสัตว์เลี้ยง (เชื่อมโยงกับ `owners`) |
| `appointments` | ข้อมูลใบนัดหมาย (เชื่อมโยงกับ `owners`, `pets`, `veterinarians`) |
| `treatments` | ข้อมูลประวัติการรักษา (เชื่อมโยงกับ `pets`, `veterinarians`, และอ้างอิง `appointments` ได้) |

---

## ⚖️ ใบอนุญาต (License)

โปรเจกต์นี้เผยแพร่ภายใต้ **MIT License** ดูรายละเอียดเพิ่มเติมในไฟล์ [LICENSE](https://github.com/James28Dev/Pet-Clinic-Webapp/blob/main/LICENSE).
